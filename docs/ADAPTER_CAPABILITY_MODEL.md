# Adapter Capability Model

Adapters declare discover, resolve, bounded_probe, preview_window, preview_tile, materialize, metadata, authentication, redirect, range, spatial subset, and temporal subset capabilities. Each adapter capability contract is hashed. Unsupported operations fail closed rather than being silently claimed.
