# Source Allowlist Policy

Only documented, allowlisted, capability-verified sources may participate in networked preview generation. Entries classified static_verified, service_discovered, or api_discovered may run without credentials. Credential-gated sources fail closed when credentials are unavailable. Future-unverified sources never execute.
