runnable_sources:
  - source_id: chirps_daily_precipitation
    source_label: CHIRPS daily precipitation GeoTIFF.GZ
    adapter: static_http_range
    access_pattern: static_https_range
    credential_required: false
    deterministic_url_generation: true
    bounded_probe_appropriate: true
    temporal_granularity: daily
    expected_magic: gzip
    expected_content_family: gzip
    url_template: "https://data.chc.ucsb.edu/products/CHIRPS-2.0/global_daily/tifs/p05/{year}/chirps-v2.0.{year}.{month}.{day}.tif.gz"
    required_params: [year, month, day]
    default_params: {year: 2023, month: "01", day: "01"}
    max_bytes: 65536
    notes: "CHIRPS daily GeoTIFF.GZ range probe using deterministic date path."
  - source_id: gridmet_daily
    source_label: gridMET daily precipitation NetCDF
    adapter: static_http_range
    access_pattern: static_https_range
    credential_required: false
    deterministic_url_generation: true
    bounded_probe_appropriate: true
    temporal_granularity: daily
    expected_magic: [netcdf, hdf5]
    expected_content_family: [netcdf, hdf5]
    url_template: "https://www.northwestknowledge.net/metdata/data/pr_{year}.nc"
    required_params: [year]
    default_params: {year: 2023}
    max_bytes: 65536
    notes: "gridMET annual precipitation NetCDF bounded range probe."
  - source_id: terraclimate_monthly
    source_label: TerraClimate monthly precipitation NetCDF
    adapter: static_http_range
    access_pattern: static_https_range
    credential_required: false
    deterministic_url_generation: true
    bounded_probe_appropriate: true
    temporal_granularity: monthly
    expected_magic: [netcdf, hdf5]
    expected_content_family: [netcdf, hdf5]
    url_template: "https://climate.northwestknowledge.net/TERRACLIMATE-DATA/TerraClimate_ppt_{year}.nc"
    required_params: [year]
    default_params: {year: 2023}
    max_bytes: 65536
    notes: "TerraClimate annual monthly precipitation NetCDF bounded range probe."
  - source_id: worldclim_bioclim_normals
    source_label: WorldClim precipitation normals ZIP
    adapter: static_http_range
    access_pattern: static_https_range
    credential_required: false
    deterministic_url_generation: true
    bounded_probe_appropriate: true
    temporal_granularity: normals
    expected_magic: zip
    expected_content_family: zip
    url_template: "https://geodata.ucdavis.edu/climate/worldclim/2_1/base/wc2.1_10m_prec.zip"
    required_params: []
    default_params: {}
    max_bytes: 65536
    notes: "WorldClim v2.1 10 minute monthly precipitation normals ZIP bounded range probe."
contract_fixtures:
  - source_id: prism_daily_ppt_static_zip
    source_label: PRISM daily precipitation historical ZIP fixture
    adapter: static_http_range
    access_pattern: static_https_range
    credential_required: false
    deterministic_url_generation: true
    bounded_probe_appropriate: true
    variable: ppt
    temporal_granularity: daily
    execution_status: fixture_only
    live_probe_enabled: false
    historical_http_status: 206
    historical_bytes_read: 65536
    historical_content_type: application/zip
    historical_detected_magic: zip
    historical_sha256_short: cc89306d4d5b
    historical_observed_at_utc: "2026-07-08T21:29:55Z"
    current_endpoint_status: unresolved_or_stale
    promotion_status: preserve_as_contract_fixture
    expected_magic: zip
    expected_content_family: zip
    unresolved_url_template_metadata: "Historical static ZIP path evidence exists, but current deterministic PRISM static/service candidates did not reproduce a bounded 206 ZIP response."
    required_params: [year, month, day, yyyymmdd]
    default_params: {year: 2023, month: "01", day: "01", yyyymmdd: "20230101"}
    max_bytes: 65536
    reason: "Historical bounded ZIP evidence exists, but current deterministic URL reproduction failed across the audited static/service candidates."
    next_unlock: "Resolve current PRISM catalog/service asset strategy before enabling live adapter execution."
    notes: "Fixture-only contract evidence; not a runnable Wave 1 live adapter source."
