# Copernicus CDSE credential-reference Sauce Pack

This example contains only the opaque credential reference
`copernicus-production`. It contains no bearer token, OAuth response, cookie,
header, signed URL, or secret-derived hash. Offline planning ends in
`CREDENTIAL_REQUIRED`; the public probe command stops before network access
because no compatible credential resolver is public.
