# Verified local raster Source Pack fixture

`fixture.tif` is a deterministic 2-by-2 synthetic categorical GeoTIFF. The
manifest pins its SHA-256 and repeats the explicit CRS, semantic type, nodata,
mask, media, role, and safe-resampling contract. The path alone is not treated
as evidence of scientific meaning.
