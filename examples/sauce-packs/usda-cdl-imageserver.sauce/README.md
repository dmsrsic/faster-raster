# USDA CDL ImageServer Source Pack fixture

This fixture reuses only the endpoint, export operation, CRS, dimension, year,
semantic-type, and resampling facts already present in the checked-in source
registry and source atlas. Validation and plan compilation are offline and do
not query the service.
